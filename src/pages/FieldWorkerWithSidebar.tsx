import { LayoutWithSidebar } from "@/components/LayoutWithSidebar";
import FieldWorker from "./FieldWorker";

const FieldWorkerWithSidebar = () => {
  return (
    <LayoutWithSidebar>
      <FieldWorker />
    </LayoutWithSidebar>
  );
};

export default FieldWorkerWithSidebar;