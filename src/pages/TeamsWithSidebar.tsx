import { LayoutWithSidebar } from "@/components/LayoutWithSidebar";
import Teams from "./Teams";

const TeamsWithSidebar = () => {
  return (
    <LayoutWithSidebar>
      <Teams />
    </LayoutWithSidebar>
  );
};

export default TeamsWithSidebar;